import React from 'react';
import { AppBar, Toolbar } from '@mui/material';

export const Header = () => {
  return (
    <AppBar position={'static'}>
      <Toolbar>
        <div>logo</div>
      </Toolbar>
    </AppBar>
  );
};
